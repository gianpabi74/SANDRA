# RB-000069B1 — Canonicalize Continuity and PVE runtime sources

- Run ID: `RB-000069B1-20260723T084920Z-1ae25b4a`
- Stato previsto: `PASS`
- Modifiche remote all'Habitat: `NONE`
- Nuove tecnologie: `NONE`

## Problema osservato

L'audit `RB-000069A2` ha dimostrato che:

- `continuity.sh` era presente come modulo operativo ma non possedeva
  una sorgente canonica sotto `src/knowledge`;
- il provider PVE era operativo ma assente da
  `src/providers/pve`;
- la ricostruibilità completa dal repository non era quindi
  dimostrata.

## Modifica

- acquisito `src/knowledge/continuity.sh` dal modulo operativo;
- sostituito l'entrypoint root con il symlink relativo
  `continuity.sh -> src/knowledge/continuity.sh`;
- acquisiti `src/providers/pve/VERSION` e
  `src/providers/pve/provider.sh` dal runtime installato;
- verificata l'identità byte-per-byte fra runtime e sorgenti;
- aggiornati architettura e stato canonico;
- rigenerate e validate le viste.

## Perimetro

La transazione ha interessato esclusivamente SANDRA locale,
Knowledge e repository GitHub.

Nessun target remoto dell'Habitat è stato interrogato o modificato.

## Gate

Il gate resta `RB-000069 — Architecture Baseline V2`.

Il prossimo intervento deve rimuovere dal generatore i dati operativi
del gate e trasferirli integralmente in `STATE.json`.
